#include <stdio.h>
int main()
{
int c;

#if 0
     while((c = getchar()) != EOF != 0 != 1)
        {
        putchar(c);
        }
//putchar(c);
#endif
//printf("%d\n", ((c = getchar()) != EOF) );
printf("%d\n", (20 != 200 ) );
return 0;
}
